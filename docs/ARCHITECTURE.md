# 项目架构

## 概述

生存确认服务采用分层架构设计，确保代码的可维护性和可扩展性。

## 核心组件

### 1. Web服务层 (`src/web/`)
- **职责**: 提供HTTP服务和API接口
- **组件**: 
  - 路由管理
  - 中间件处理
  - 静态文件服务
  - 模板渲染

### 2. API处理层 (`src/api/`)
- **职责**: 处理HTTP请求和响应
- **组件**:
  - 请求处理器
  - 响应格式化
  - 错误处理
  - 参数验证

### 3. 业务逻辑层 (`src/state/`)
- **职责**: 状态管理和业务逻辑
- **组件**:
  - 状态存储
  - 业务规则处理
  - 数据验证
  - 事务管理

### 4. 服务层
- **邮件服务** (`src/email/`): 处理邮件发送
- **配置服务** (`src/config/`): 配置文件管理和验证

## 数据流架构

```
用户请求 → Web路由 → API处理器 → 业务逻辑 → 内存状态
    ↓
定时任务 → 状态检查 → 邮件发送 → 配置文件读取
```

## 关键设计模式

### 1. 依赖注入
- 使用依赖注入管理服务之间的依赖关系
- 提高代码的可测试性和可维护性

### 2. 中间件模式
- Web请求处理使用中间件进行横切关注点处理
- CORS、日志、认证等通过中间件实现

### 3. 配置驱动
- 所有配置通过YAML文件管理
- 支持热重载和环境变量覆盖

### 4. 内存存储
- 签到时间存储在内存中
- 使用读写锁保证线程安全

## 项目目录结构

```
good-bye/
├── cmd/
│   └── main.go              # 应用程序入口点
├── src/
│   ├── api/                 # API接口层
│   │   ├── api_handler.go   # API请求处理器
│   │   └── models.go       # 数据模型
│   ├── config/              # 配置管理
│   │   ├── config_manager.go # 配置管理器
│   │   └── config.go        # 配置结构定义
│   ├── email/               # 邮件服务
│   │   ├── email_service.go # 邮件发送服务
│   │   └── templates/       # 邮件模板
│   ├── scheduler/           # 任务调度器
│   │   └── scheduler.go     # 定时任务管理
│   ├── state/               # 状态管理
│   │   └── state_manager.go # 状态管理器
│   └── web/                 # Web服务层
│       └── router.go        # 路由配置
├── templates/               # HTML模板
│   └── checkin.html        # 签到页面模板
├── static/                  # 静态资源
│   ├── css/                # CSS样式文件
│   ├── js/                 # JavaScript文件
│   └── images/             # 图片资源
├── config/                  # 配置文件
│   ├── config.yaml         # 默认配置
│   └── config.local.yaml   # 本地配置（不提交到git）
├── data/                    # 数据文件
│   └── posthumous_papers.md # 遗书内容文件
├── logs/                    # 日志文件
├── builds/                  # 构建输出
├── docs/                    # 文档目录
├── .github/                 # GitHub配置
└── Makefile                # 构建脚本
```

## 核心模块说明

### 1. 配置管理模块
- **文件**: `src/config/config_manager.go`
- **功能**: 统一管理应用配置
- **特性**: 支持热重载、环境变量、配置验证

### 2. 状态管理模块
- **文件**: `src/state/state_manager.go`
- **功能**: 管理用户签到状态和系统状态
- **特性**: 内存存储、线程安全、状态变更记录

### 3. 邮件服务模块
- **文件**: `src/email/email_service.go`
- **功能**: 处理邮件发送逻辑
- **特性**: SMTP支持、模板渲染、队列管理

### 4. 任务调度模块
- **文件**: `src/scheduler/scheduler.go`
- **功能**: 定时检查用户状态和发送邮件
- **特性**: Cron表达式、错误重试、优雅退出

### 5. Web服务模块
- **文件**: `src/web/router.go`
- **功能**: 提供Web界面和API接口
- **特性**: RESTful API、中间件支持、静态文件服务

## 技术选型理由

### 1. Go语言
- **高性能**: 适合高并发场景
- **简洁**: 语法简单，学习成本低
- **跨平台**: 支持多平台编译
- **丰富生态**: 有成熟的第三方库

### 2. Gin框架
- **轻量级**: 性能优秀
- **中间件**: 灵活的中间件机制
- **路由**: 强大的路由功能
- **社区**: 活跃的社区支持

### 3. Viper配置管理
- **多格式**: 支持YAML、JSON、TOML等
- **环境变量**: 支持环境变量覆盖
- **热重载**: 支持配置热重载
- **验证**: 内置配置验证

### 4. 内存存储
- **简单**: 无需数据库依赖
- **快速**: 内存访问速度快
- **轻量**: 减少部署复杂度
- **适合**: 当前业务场景足够

## 扩展性考虑

### 1. 水平扩展
- 无状态设计，支持多实例部署
- 可以通过负载均衡器分发请求

### 2. 功能扩展
- 模块化设计，便于添加新功能
- 插件化架构，支持第三方集成

### 3. 存储扩展
- 当前使用内存存储，未来可扩展到数据库
- 配置管理支持多种存储后端

### 4. 监控扩展
- 预留健康检查接口
- 支持日志收集和监控